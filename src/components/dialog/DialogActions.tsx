import React from "react";
import Button from "components/Button";
import {StyledDialogActions} from "styles/components/dialog/StyledDialogActions";

const DialogActions = () => {
  return (
    <StyledDialogActions>
      <Button>
        Submit
      </Button>
    </StyledDialogActions>
  )
}

export default DialogActions
